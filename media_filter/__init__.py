"""
Модуль медиафильтра.

Отдельная ветка обработки Q&A-запроса, когда пользователь включил в UI
чекбокс «Медиафильтр». Не идёт через стандартный run_qa() — вместо
обычного LLM-summary мы:
  1) тянем из Telegram ТОЛЬКО медиа-сообщения выбранных типов в окно
     времени (через messages.search SearchRequest, а не iter_messages);
  2) опционально дёргаем LLM-парсер на свободный текст пользователя,
     чтобы вытащить структурные ограничения (размер, длительность, ...);
  3) применяем структурный пост-фильтр поверх собранных метаданных;
  4) опционально дёргаем LLM-реранкер по смыслу (только если в запросе
     осталась семантическая часть после парсера);
  5) собираем ответ как карточки сообщений, сгруппированные по типу
     медиа (или сначала по чату, потом по типу — в групповом режиме).

Подробности — `Desktop/CoTel/MediaFilter/architecture-media-filter.md`.
"""
